{
  "code": [
    {
      "id": "c1",
      "key": "OrderService",
      "name": "OrderService.java",
      "language": "java",
      "content": "public class OrderService {\n\n  public Order createOrder(OrderRequest req) {\n    // acquires a connection for the WHOLE retry sequence below\n    try (Connection conn = pool.acquire()) {\n      PaymentResult result = paymentClient.charge(req, conn);\n      return orderRepo.save(req, result, conn);\n    }\n  }\n}"
    }
  ]
}